
```
# SPDX-License-Identifier: Apache-2.0

cmake_minimum_required(VERSION 3.20.0)

find_package(Zephyr REQUIRED HINTS $ENV{ZEPHYR_BASE})
project(sockets_echo)

FILE(GLOB app_sources
    src/main.c
    src/para/eep_para.c
    )
FILE(GLOB driver_sources
        src/driver/led/net_led.c
        src/driver/key/key.c
        src/driver/eeprom/eep.c
        src/driver/rtc/rtc.c
    )

FILE(GLOB net_sources
    src/net/init/net_init.c
    src/net/init/net_l2_event.c
    # src/net/work/socket_tcp.c
    src/net/modules/dhcp/DHCPv4.c
    src/net/modules/dns/dns.c
    src/net/modules/static/static_ipv4.c
    src/net/modules/mac/mac.c
    src/net/para/socket_cfg.c
    src/net/http/http_init.c

    src/net/http/http_query.c
    src/net/http/http_parser.c
    )
zephyr_library_sources_ifdef(CONFIG_ENABLE_BT_MODULE src/driver/bt/ble_module.c)

target_sources(app PRIVATE ${app_sources} ${driver_sources} ${net_sources})

zephyr_include_directories(src
    src/driver/bt
    src/driver/key
    src/driver/led
    src/driver/lora
    src/driver/rtc
    src/driver/eeprom
    src/para
    src/net
    src/net/modules/dhcp
    src/net/modules/dns
    src/net/modules/static
    src/net/modules/mac
    src/net/init
    src/net/http
)

#kconfig_dir(${CMAKE_CURRENT_SOURCE_DIR})
#add_subdirectory(src)
```
