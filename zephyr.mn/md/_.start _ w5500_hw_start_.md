.start 与 .stop

触发时机：

.start：当接口被激活时（例如执行 net_if_up() 或手动通过 net shell 输入 net iface up）。

.stop：当接口被禁用时（执行 net_if_down()）。

作用：控制硬件的物理收发开关。

你的任务：在 .start 中打开 CH395 的中断屏蔽位、进入 RAW 模式收发状态；在 .stop 中关闭中断并让芯片进入低功耗模式。